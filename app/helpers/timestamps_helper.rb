module TimestampsHelper
      #def last_timestamp_closed?
      #    timestamp = current_user.timestamps.order('created_at DESC').first
      #    unless timestamp.nil?
      #        !timestamp.end_time.blank?
      #    else
      #        return true
      #    end
      #end
end
