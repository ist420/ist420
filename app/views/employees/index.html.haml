%table.table-hover.table
  %thead
    %tr
      %th
        Employee ID
      %th
        Employee Name
  %tbody
    - @employees.each do |e| 
      %tr
        %td
          = e.id
        %td
          = e.name
       
  
